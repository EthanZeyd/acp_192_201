#rental.py
-How to add renter in return_vehicle to handle returned unauthorized renters and vehicles? 

#main.py
-Explain undefined return_vehicle funnction.
-However, return_vehicle function is already has in rental.py. Why do i need to create a new return_vehicle in this test_run file?
-Why my child class is not working and showing assertion error whereas my parent class 'Vehicle" is working?
-how did return_vehicle work when input(renter, car) and how it supposed to work when 'assert' added?
-how it battery_capacity work when an object is created using ElectricCar inheriting its parent class?
-how we're gonna insert sample battery_capacity and engine_capacity to flow in Vehicle's child classes? Is the assertion wrong or are inherited classes wrong? 
-Is the class handle mistakely the input as strings? like "1000" cc?
-Is __str__() function necessarily required in both ElectricCar and Motorbike classes?
